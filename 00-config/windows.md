# Dicas do que ser feito em um ambiente windows

## Prints de monitoramente nativo do windows

Encontrei na internet alguns comentarios sobre uma ferramenta implementada nativamente no windows 11 que gera capturas de tela de forma automática.
Para desativar:

```cmd
    DISM /Online /Disable-Feature /FeatureName:Recall
```
